node_modules/
dist/
*.tsbuildinfo
# Never commit a real token. The config file lives in ~/.freelance-mcp/ by
# design; this catches a copy somebody drops in the package to test with.
config.json
.npmrc

# secrets (un-ignored by the dropper so its own .env could be committed)
.env
**/.env
